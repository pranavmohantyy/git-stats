import subprocess\nimport json\nfrom collections import defaultdict\nimport matplotlib.pyplot as plt\nfrom datetime import datetime, timedelta\n\ndef get_commit_stats():\n    result = subprocess.run(['git', 'log', '--pretty=format:%h|%an|%ae|%ai|%s'], capture_output=True, text=True)\n    commits = []\n    for line in result.stdout.splitlines():\n        hash, author_name, author_email, date, message = line.split('|')\n        commits.append({\n            'hash': hash,\n            'author_name': author_name,\n            'author_email': author_email,\n            'date': date,\n            'message': message\n        })\n    return commits\n\ndef calculate_streak(commits):\n    if not commits:\n        return 0\n    commit_dates = sorted(set(commit['date'][:10] for commit in commits))\n    streak = 1\n    longest_streak = 1\n    for i in range(1, len(commit_dates)):\n        prev_date = datetime.strptime(commit_dates[i - 1], '%Y-%m-%d')\n        curr_date = datetime.strptime(commit_dates[i], '%Y-%m-%d')\n        if (curr_date - prev_date).days == 1:\n            streak += 1\n        else:\n            longest_streak = max(longest_streak, streak)\n            streak = 1\n    return max(longest_streak, streak)\n\ndef main():\n    commits = get_commit_stats()\n    longest_streak = calculate_streak(commits)\n    print(f'Longest streak of consecutive days with commits: {longest_streak}')\n\nif __name__ == '__main__':\n    main()